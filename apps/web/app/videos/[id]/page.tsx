import Link from 'next/link';
import { notFound } from 'next/navigation';
import { requireSession } from '../../../lib/auth';
import { loadRun } from '../../../lib/data';

export const dynamic='force-dynamic';
const money=(v:unknown)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:2}).format(Number(v||0));
const pretty=(v:unknown)=>JSON.stringify(v??{},null,2);
const pill=(state:unknown)=>{const s=String(state||'').toLowerCase();return s.includes('pass')||s.includes('ready')||s.includes('public')?'good':s.includes('fail')||s.includes('block')?'bad':'warn';};

export default async function VideoPage({params}:{params:Promise<{id:string}>}){await requireSession();const {id}=await params;const run:any=await loadRun(id);if(!run)notFound();const attention=run.qa_report?.attention??run.metadata?.attention;const finalInspection=run.qa_report?.finalInspection??run.metadata?.finalInspection;const renderUri=String(run.metadata?.renderUri??'');const canPreview=renderUri.startsWith('file://')||renderUri.startsWith('/')||renderUri.startsWith('.');return <div className="content"><header className="topbar"><div><p className="eyebrow">Video review</p><h1>{run.working_title||run.angle||id}</h1></div><Link href="/" className="pill info">← Portfolio</Link></header><main className="main"><div className="detail-grid"><div><section className="video-shell">{canPreview?<video controls preload="metadata" src={`/api/videos/${id}/stream`}/>:<div className="muted">Render no disponible en almacenamiento local.</div>}</section><section className="section card"><div className="row"><div><p className="eyebrow">Readiness</p><h2>Autorevisión final</h2></div><span className={`pill ${pill(run.state)}`}>{run.state}</span></div><div className="metric-grid" style={{gridTemplateColumns:'repeat(4,minmax(0,1fr))'}}><div className="metric"><div className="label">QA</div><div className="value">{run.qa_score??'—'}</div></div><div className="metric"><div className="label">Attention</div><div className="value">{attention?.score??'—'}</div></div><div className="metric"><div className="label">Render QA</div><div className="value">{finalInspection?.score??'—'}</div></div><div className="metric"><div className="label">Opportunity</div><div className="value">{run.opportunity_score??'—'}</div></div></div>{attention?.dimensions?.length?<div className="list" style={{marginTop:14}}>{attention.dimensions.map((d:any)=><div className="list-item" key={d.id}><div><strong>{d.id}</strong><div className="fine">{d.message}</div></div><span className={`pill ${d.score>=85?'good':d.score>=70?'warn':'bad'}`}>{Math.round(d.score)}</span></div>)}</div>:null}</section></div><aside><section className="card"><p className="eyebrow">Economics</p><h2>Economía</h2><div className="list"><div className="list-item"><span>Coste</span><strong>{money(run.economic_cost??run.total_cost_usd)}</strong></div><div className="list-item"><span>Revenue</span><strong>{money(run.total_revenue_usd)}</strong></div><div className="list-item"><span>Profit</span><strong>{money(run.profit_usd)}</strong></div><div className="list-item"><span>ROI</span><strong>{run.roi==null?'—':`${Number(run.roi).toFixed(2)}x`}</strong></div><div className="list-item"><span>Watch min / $</span><strong>{run.watch_minutes_per_dollar==null?'—':Number(run.watch_minutes_per_dollar).toFixed(1)}</strong></div></div></section><section className="section card"><p className="eyebrow">Provider ledger</p><h2>Coste desglosado</h2><div className="list">{run.costs?.map((c:any)=><div className="list-item" key={`${c.stage}:${c.provider}:${c.model}`}><div><strong>{c.stage}</strong><div className="fine">{c.provider} · {c.model}</div></div><strong>{money(c.cost)}</strong></div>)}</div></section>{run.youtube_video_id?<section className="section card"><p className="eyebrow">YouTube</p><h2>Private upload</h2><a className="run-link" target="_blank" rel="noreferrer" href={`https://www.youtube.com/watch?v=${run.youtube_video_id}`}>Abrir vídeo privado ↗</a><div className="fine" style={{marginTop:8}}>Estado: {run.publication_state}</div></section>:null}</aside></div><section className="section split"><div className="card"><p className="eyebrow">Script</p><h2>Guion final</h2><div className="code-box">{pretty(run.script)}</div></div><div className="card"><p className="eyebrow">Final inspection</p><h2>Media QA</h2><div className="code-box">{pretty(finalInspection)}</div></div></section></main></div>}
